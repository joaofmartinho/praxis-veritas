# Transmutations

This directory stores required non-canonical transmutation receipts.

Each transmutation receipt should stay concise and record what changed in `Veritas`, which project instruction files were updated, and which vault artifacts were consumed.
