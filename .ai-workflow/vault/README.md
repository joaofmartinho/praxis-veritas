# Vault

This directory stores non-canonical run documents, run records, and archived workflow artifacts.

`vault/` is not canonical knowledge. It exists for provenance, auditability, and human context. Future agents should read `Veritas` first and only inspect `vault/` when they need historical reasoning that is not already captured canonically.
